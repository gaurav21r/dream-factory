import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.swarise.dreamfactory',
  appName: 'dream-factory',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
